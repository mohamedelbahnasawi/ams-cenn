.ipynb_checkpoints
__pycache__
*.egg-info
Gemfile*
Gemfile.lock
docs/_site
build
dist
.vscode
*.gif
*.csv
*/data/*
*.parquet
tmp
_docs/
sidebar.yml
.DS_Store
.gitattributes
.gitconfig
nbs/.last_checked
_proc
lightning_logs/
/**/data/
debug_run

longhorizon/
_old/
data
mlruns
checkpoints
nbs/_extensions/mintlify
*.env
.coverage*

# ignore files created in the docs folder
docs/**/*.mdx

docs-scripts
docs/mintlify/examples

nbs/_extensions
.quarto

*.png
# README assets are checked in (override the global *.png ignore)
!assets/architecture.png

.venv/
# --- CeNN fork additions ---
.venv/
data/
experiments/results/
experiments/checkpoints/
experiments/published/
experiments/aggregated/
experiments/artifacts/
experiments/efficiency/

# debug scratch
experiments/_probe_*

# transient preflight status dump (regenerated by watch_preflight.py each run)
experiments/preflight_status.json

# local LaTeX engine (tectonic.exe) used to render TikZ figures — do not commit (49MB binary)
experiments/.tools/

# W&B local run cache (2GB; synced to cloud; never commit)
wandb/

# e2e plumbing-test scratch output (CENN_EXP_DIR)
_e2e_scratch/

# worst-case probe scratch (big-dataset OOM/timing probe)
_probe_scratch/

# L-sensitivity ablation outputs (CENN_INPUT_SIZE != 512 -> nested L{N}/ isolation)
experiments/L[0-9]*/
experiments/V[0-9]*/

# pre-protocol results quarantined (val_check_steps=50 era; re-run at locked protocol)
experiments/results_stale_preprotocol/
_wdiag/

# session scratch: experiment scratch dirs + launch logs — local only
_scaler_ab/
experiments/_scaler_ab_h200/
experiments/_undertrain3k/
experiments/_weather_minmax_backup/
*.log
